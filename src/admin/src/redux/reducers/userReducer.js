import * as userAction from '../actions/userAction';const initialState = {    id: '',    create_time: '',    openid: '',    account: '',    name: '',    email: '',    mobile: '',    qq: '',    wechat: '',    storage_left: null,    storage: '',    is_disable: 0,    type: 0,    over_time: null,    total_payment: null,    head_url: null,    token: '',    isReset: false,    isChecked:false,};export default function userReducer(state = initialState, action) {    switch (action.type) {        case userAction.LOGIN:            return Object.assign({}, state, action.payload);        case userAction.REGISTER:            return Object.assign({}, state, action.payload);        case userAction.LOGOUT:            if(state.isChecked){                initialState.account = state.account;                initialState.isChecked = state.isChecked;            }else{                initialState.account = '';                initialState.isChecked = false;            }            return Object.assign({}, state,initialState);        case userAction.RESET_PASSWORD:            return Object.assign({}, state, action.payload);        case userAction.DELETE_ACCOUNT:            return Object.assign({},state,initialState);        case userAction.SAVE_CHANGES:            return Object.assign({}, state, action.payload);        default:            return state;    }}